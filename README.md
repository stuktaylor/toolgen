
- To run:

* Copy the .env.example file to .env
* Update the .env file with a session secret and OPENAI_API_KEY 

$ npm install
$ npm run start

Access via http://localhost:3000

Create a `.env` file in the project root to set runtime configuration:

PORT=3000
SESSION_SECRET=replace-me
